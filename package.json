{
  "name": "school-erp-project",
  "version": "1.0.0",
  "description": "Premium School ERP Platform",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "install": "cd frontend && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.x"
  }
}
